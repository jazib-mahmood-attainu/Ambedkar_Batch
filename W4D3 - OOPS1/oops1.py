"""
class : are tool to help us model real world into code.

classes have attribues and methods
attributes are the characteristics that define my class
methods are the actions my objects/instance of my class can do.

objects are instance of my class.

Rule 1: 
Each class, should be a separate file.

Rule 2:
filename should be classname , in small letters, separated by _(underscore)

Rule 3:
class name should always be in CamelCase.

sahilarora => SahilArora

Rule 4:

every class has a dunder init function,
__init__

Rule 5:
All functions inside the class will have a parameter "self",
as first parameter.

"""